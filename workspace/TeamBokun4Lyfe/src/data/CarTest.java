package data;

import static org.junit.Assert.*;

import org.junit.Test;

public class CarTest {

	@Test
	public void test() {
		fail("Not yet implemented");
		/*
		 * Test that the car class stores the appropriate private data members:
		 * int			id
		 * double		speed
		 * String		direction
		 * String		onOffRamp
		 * String		freeway
		 * int 			hour;
		 */
		/*
		 * Test that this class is fully encapsulated (except private data member "hour"
		 * will not have a setter as each car is only created at 1 instance of time;
		 * it will have a getter though)
		 * This means that there must be functional getters and setters for all other 
		 * private data members
		 */
		/*
		 * Test that the toString method is overridden properly and prints our correct
		 */
	}

}
